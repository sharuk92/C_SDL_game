#include <iostream>
#include "Game.h"
#include "LEditor.h"

using namespace std;

int main()
{
	 
	 LEditor editor("Level Editor");
		
    while(/*game.isRunning()*/ editor.isRunning())
    {
		  editor.draw();
		  editor.getInput();
    }


    return 0;
}
